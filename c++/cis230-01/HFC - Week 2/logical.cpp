#include <iostream>

int main() {

    // 0 for false and 1 for true
    std::cout<<(1 > 3 && 7 < 9)<<std::endl;
    std::cout<<(1 < 3 || 4 == 4)<<std::endl;
    std::cout<<(!(1 >= 3))<<std::endl;
    
    return 0;
}