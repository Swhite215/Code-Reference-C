#include <iostream>

int main() {

    double apple;

    std::cout<<"The size of a short integer is "<< sizeof(short) << " bytes."<<std::endl;
    std::cout<<"The size of a long integer is "<< sizeof(long) << " bytes."<<std::endl;
    std::cout<<"An apple can be eaten in  "<< sizeof(apple) << " bytes!"<<std::endl;

    return 0;
}