#include <iostream>
#include <cmath>

int main () {

    int y = abs(-4);
    double cosA = cos(12);
    double exponential = exp(10);
    double remainder = fmod(10, 3);
    double logA = log(1);
    double logB = log10(1);
    double power = pow(3, 2);
    int rounded = round(10.11);
    double sinA = sin(12);
    double squareRoot = sqrt(4);
    double tanA = tan(12);

    return 0;
}